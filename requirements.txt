# M²-Alpha public release runtime dependencies

# Data / computation
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
requests>=2.31

# A-share public data sources, no token required
baostock>=0.9
akshare>=1.18
efinance>=0.5.8

# Deep-learning inference, CPU wheel by default
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0,<3.0
